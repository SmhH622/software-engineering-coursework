import ControlClasses.Refresh;
import GUI.Company.CompanyPerson;
import GUI.Customer.bills;
import GUI.Customer.init;

public class Start {
    @SuppressWarnings("unchecked")
    public static void main(String args[]) throws Exception {
     new init();
     new Refresh();
        //   new CompanyPerson("C001");
    }
}
